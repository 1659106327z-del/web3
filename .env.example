# SQLite 数据库文件路径（相对于项目根目录）
DATABASE_URL="file:./dev.db"

# JWT 签名密钥（至少 32 字节的随机字符串）
# 生成建议：openssl rand -base64 48  或  node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
AUTH_SECRET="change-me-in-production-use-at-least-32-random-bytes-xyz"
